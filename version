commit_count=$(shell git rev-list --count HEAD)
rev_id=$(shell git rev-parse --short HEAD)
#################################################
channels_splitter_version?=0.1.$(commit_count)-$(rev_id)
join_results_version?=0.2.$(commit_count)-$(rev_id)
tools_version?=0.1.$(commit_count)-$(rev_id)

